# Benchmarks

Planned benchmarks cover transform propagation, dirty-subtree evaluation,
motion integration, dependency-level scheduling, and engine synchronization.

Benchmarks must report node count, hierarchy shape, changed-node ratio,
allocation behavior, and execution policy.
