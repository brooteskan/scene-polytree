# Examples

The first executable example will be an articulated low-poly tank with hull,
turret, and gun nodes. Player and AI controllers will write different motion
intent into the same scene representation.

Examples will be added after the external polytree package is available so they
demonstrate the real traversal vocabulary rather than a temporary local loop.
